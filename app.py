import streamlit as st
from google import genai
from google.genai.errors import APIError
import pandas as pd
import io

# --- 1. ข้อมูลคงที่ (Constants) ---
AIRCRAFT_DATA = {
    "A320neo": {"eco": 156, "bc": 8, "first": 0, "fuel_cost": 708, "range_km": 6300},
    "A321neo": {"eco": 162, "bc": 12, "first": 0, "fuel_cost": 840, "range_km": 7400},
    "A350-900": {"eco": 288, "bc": 40, "first": 0, "fuel_cost": 1950, "range_km": 15000},
    "A350-900ULR": {"eco": 133, "bc": 48, "first": 8, "fuel_cost": 2095, "range_km": 18000},
    "B787-8": {"eco": 261, "bc": 30, "first": 0, "fuel_cost": 1370, "range_km": 13500},
    "B787-9": {"eco": 297, "bc": 36, "first": 0, "fuel_cost": 1650, "range_km": 14000},
    "B777-300ER": {"eco": 315, "bc": 40, "first": 8, "fuel_cost": 2080, "range_km": 13650},
}

CONTINENTS = [
    "Africa", "Antarctica", "Asia", "Europe", "North America", "Oceania", "South America"
]

# --- 2. การตั้งค่าหน้าเว็บและ Sidebar ---
st.set_page_config(
    page_title="✈️ Airline Route Calculator (Gemini Powered)",
    layout="wide",
    initial_sidebar_state="expanded"
)

# Sidebar สำหรับ API Key
with st.sidebar:
    st.image("https://upload.wikimedia.org/wikipedia/commons/thumb/d/d4/Air_transport_icon_with_aeroplane.svg/1200px-Air_transport_icon_with_aeroplane.svg.png", width=100)
    st.title("⚙️ การตั้งค่า API")

    # ช่องรับ Gemini API Key
    gemini_api_key = st.text_input(
        "**Google Gemini API Key**",
        type="password",
        help="API Key สำหรับเรียกใช้ Google Gemini."
    )

    if 'gemini_api_key' not in st.session_state:
        st.session_state.gemini_api_key = ""
        
    st.session_state.gemini_api_key = gemini_api_key

# --- 3. ฟังก์ชันจัดการ Gemini Client (แก้ไข: ใช้ @st.cache_resource) ---

@st.cache_resource(show_spinner="กำลังตั้งค่า Gemini Client...")
def get_gemini_client(api_key: str):
    """
    สร้างและแคชออบเจกต์ Gemini Client
    ใช้ @st.cache_resource เพื่อหลีกเลี่ยง UnhashableParamError
    """
    if not api_key:
        return None
    try:
        # Client จะถูกสร้างใหม่เมื่อ API key เปลี่ยน
        return genai.Client(api_key=api_key)
    except Exception as e:
        st.error(f"เกิดข้อผิดพลาดในการตั้งค่า Gemini Client: {e}")
        return None

# ตรวจสอบ Client สถานะ
client = get_gemini_client(st.session_state.gemini_api_key)
is_gemini_ready = client is not None and st.session_state.gemini_api_key

# --- 4. ฟังก์ชันเรียกใช้ Gemini API (แก้ไข: ลบ client ออกจากพารามิเตอร์ของ @st.cache_data) ---

def _get_active_client():
    """เรียกใช้ Client ที่แคชไว้ภายในฟังก์ชันที่ถูกแคชข้อมูล"""
    return get_gemini_client(st.session_state.gemini_api_key)

@st.cache_data(show_spinner="กำลังตรวจสอบความสอดคล้องของข้อมูล...")
def check_airport_consistency(icao_code: str, city_name: str, continent: str):
    """
    1.2: ให้ Gemini เช็คความสอดคล้องของข้อมูลสนามบิน
    * client ถูกเรียกใช้ภายในฟังก์ชันเพื่อหลีกเลี่ยงการแคช client *
    """
    client = _get_active_client()
    if client is None:
        return "API_ERROR: Gemini Client ไม่พร้อมใช้งาน"

    prompt = (
        f"ตรวจสอบความสอดคล้องของข้อมูลสนามบิน: ICAO Code: {icao_code}, City: {city_name}, Continent: {continent}. "
        "ถ้าข้อมูลสอดคล้อง (ตรงตามโลกจริง) ให้ตอบว่า 'PASS'. ถ้าไม่สอดคล้อง ให้ตอบว่า 'FAIL: [คำอธิบายว่าทำไมไม่ตรงกัน]'. "
        "เช่น 'FAIL: DMK คือ Bangkok แต่ Continent เป็น Europe'"
    )
    
    # --- Mock Response สำหรับ Demo (เพื่อลดการเรียกใช้ API ซ้ำ) ---
    if icao_code.upper() == "BKK" and city_name.lower() == "bangkok" and continent == "Asia":
        return "PASS"
    elif icao_code.upper() == "DMK" and city_name.lower() == "bangkok" and continent == "Asia":
        return "PASS"
    elif icao_code.upper() == "LHR" and city_name.lower() == "london" and continent == "Europe":
        return "PASS"
    
    # การเรียกใช้จริง:
    try:
        response = client.models.generate_content(
            model='gemini-2.5-flash',
            contents=prompt
        )
        return response.text.strip()
    except APIError as e:
        return f"API_ERROR: ไม่สามารถเรียกใช้ Gemini ได้: {e}"
    except Exception:
         return "FAIL: ตรวจสอบไม่ผ่าน หรือเกิดข้อผิดพลาดที่ไม่ทราบสาเหตุ"

@st.cache_data(show_spinner="กำลังคำนวณระยะทางบิน...")
def get_flight_distance(destination_icao: str):
    """
    2.1: ให้ Gemini ค้นหาระยะทางบินจาก BKK ถึงสนามบินปลายทาง (เป็นจำนวนเต็ม กิโลเมตร)
    """
    client = _get_active_client()
    if client is None:
        return 0

    prompt = (
        f"ค้นหาระยะทางบิน (Great Circle Distance) จากสนามบิน BKK (Suvarnabhumi, Bangkok, Thailand) "
        f"ไปยังสนามบินปลายทางที่มี ICAO code คือ {destination_icao}. "
        "ให้แสดงผลเฉพาะ 'ระยะทางเป็นกิโลเมตร' เท่านั้น โดยเป็น **จำนวนเต็ม** และ **ไม่ใช่ค่าประมาณ**"
    )

    # --- Mock Response สำหรับ Demo ---
    if destination_icao.upper() == "LHR":
        return 9553
    elif destination_icao.upper() == "JFK":
        return 13958
    elif destination_icao.upper() == "SYD":
        return 7523
    
    # การเรียกใช้จริง:
    try:
        response = client.models.generate_content(
            model='gemini-2.5-flash',
            contents=prompt
        )
        try:
            return int(response.text.strip())
        except ValueError:
            return 0 
    except APIError as e:
        st.error(f"API_ERROR: ไม่สามารถคำนวณระยะทางได้: {e}")
        return 0

@st.cache_data(show_spinner="กำลังคาดการณ์ Demand และประเมินความเหมาะสมของเครื่องบิน...")
def get_aircraft_evaluation(distance_km: int, destination_icao: str, destination_city: str):
    """
    2.2 & 2.3: ให้ Gemini ช่วยแนะนำเครื่องบิน คาดการณ์ Demand และประเมินความเหมาะสม
    """
    client = _get_active_client()
    if client is None:
        return None

    prompt = (
        f"จากเส้นทางบิน BKK ไปยัง {destination_city} ({destination_icao}) ซึ่งมีระยะทางบิน {distance_km} กิโลเมตร. "
        "ให้วิเคราะห์ Demand และความเหมาะสมของเครื่องบินรุ่นต่างๆ ดังนี้:\n"
        f"ข้อมูลเครื่องบิน: {AIRCRAFT_DATA}\n"
        "เกณฑ์การประเมิน Demand (2.2.8): เปรียบเทียบจากเที่ยวบินสายการบินอื่นที่มีอยู่ + ประชากร (จำนวน, ฐานะ, อาชีพ) + การท่องเที่ยว + ปัจจัยอื่นๆ\n"
        "กรุณาสร้างผลลัพธ์ในรูปแบบ **CSV (Comma Separated Values)** โดยไม่มี Header และคอลัมน์ต่างๆ ต้องเรียงตามลำดับนี้:\n"
        "1. ชื่อรุ่นเครื่องบิน (ตาม key ใน AIRCRAFT_DATA)\n"
        "2. พิสัยการบิน (กิโลเมตร) (ตาม key ใน AIRCRAFT_DATA)\n"
        "3. จำนวนที่นั่ง (eco/bc/first)\n"
        "4. อัตราสิ้นเปลือง (usd/hr)\n"
        "5. คาดการณ์ผู้โดยสารขาไปต่อสัปดาห์ (eco/bc/first) (เช่น 100/10/2)\n"
        "6. คาดการณ์ผู้โดยสารขากลับต่อสัปดาห์ (eco/bc/first)\n"
        "7. ความถี่เที่ยวบิน (ไป+กลับ) ต่อสัปดาห์ที่เหมาะสม (จำนวนเต็ม)\n"
        "8. เวลา Departure จาก BKK ที่เหมาะสม (XX:XXน., XX:XXน., ...)\n"
        "9. เวลา Departure จากสนามบินนั้นที่เหมาะสม (XX:XXน., XX:XXน., ...)\n"
        "10. ความเหมาะสม (5.0, 4.5, ..., 1.0, 0.0)\n"
        "11. สรุปสาเหตุ (50-100 คำ ภาษาไทย)"
    )

    # --- Mock Response สำหรับ Demo (ใช้ LHR เหมือนเดิม) ---
    if destination_icao.upper() == "LHR":
        mock_csv = """
A320neo,6300,156/8/0,708,200/20/0,210/25/0,0,N/A,N/A,0.0,เครื่องบินรุ่นนี้มีพิสัยการบินไม่เพียงพอที่จะบินตรงจาก BKK ไป LHR. ต้องมีการแวะเติมน้ำมันซึ่งไม่เป็นไปตามข้อกำหนดการบินระยะไกล ทำให้ไม่เหมาะสมอย่างยิ่ง
A321neo,7400,162/12/0,840,200/20/0,210/25/0,0,N/A,N/A,0.0,เครื่องบินรุ่นนี้มีพิสัยการบินไม่เพียงพอที่จะบินตรงจาก BKK ไป LHR เช่นเดียวกับ A320neo. การบินระยะไกลข้ามทวีปจำเป็นต้องใช้เครื่องบินที่มีพิสัยการบินสูงกว่า
A350-900,15000,288/40/0,1950,200/20/0,210/25/0,10,"00:05น., 09:30น., 00:05น., 09:30น., 00:05น.","19:00น., 13:00น., 19:00น., 13:00น., 19:00น.",4.5,A350-900 มีพิสัยการบินเพียงพอต่อเส้นทางนี้ และมีความจุที่นั่งที่เหมาะสมกับ Demand ปานกลางถึงสูง การจัดที่นั่งให้ความยืดหยุ่นดีเยี่ยม เหมาะสมสำหรับการบินข้ามทวีป
B787-9,14000,297/36/0,1650,200/20/0,210/25/0,12,"00:05น., 09:30น., 00:05น., 09:30น., 00:05น., 09:30น.","19:00น., 13:00น., 19:00น., 13:00น., 19:00น., 13:00น.",4.0,เป็นตัวเลือกที่สมดุลระหว่างพิสัยการบินและความจุที่นั่ง โดยมีต้นทุนการดำเนินงานที่แข่งขันได้กับ A350-900 สามารถรองรับความถี่เที่ยวบินที่สูงขึ้นได้ดี
"""
        return mock_csv
    
    # การเรียกใช้จริง:
    try:
        response = client.models.generate_content(
            model='gemini-2.5-flash',
            contents=prompt
        )
        if "```csv" in response.text:
            csv_data = response.text.split("```csv")[1].split("```")[0].strip()
        else:
            csv_data = response.text.strip()
        return csv_data
    except APIError as e:
        st.error(f"API_ERROR: ไม่สามารถประเมินเครื่องบินได้: {e}")
        return None
    except Exception as e:
        st.error(f"เกิดข้อผิดพลาดในการเรียกใช้ Gemini: {e}")
        return None

# --- 5. ส่วนหลักของแอปพลิเคชัน ---

st.title("✈️ Airline Route Calculator (Gemini Powered)")
st.caption("โปรแกรมคำนวณข้อมูลสายการบินสำหรับเส้นทางบินใหม่ โดยใช้ข้อมูลจริงจาก Google Gemini")

if not is_gemini_ready:
    st.warning("🚨 กรุณาใส่ **Google Gemini API Key** ใน Sidebar เพื่อใช้งานโปรแกรม")



st.header("1. เลือกเส้นทางบินปลายทาง")
col1, col2, col3 = st.columns(3)

# 1.1 รับ input จาก user
with col1:
    icao_code = st.text_input(
        "**ICAO Code ของสนามบินปลายทาง**",
        placeholder="เช่น LHR",
        max_chars=4
    ).upper()

with col2:
    city_name = st.text_input(
        "**ชื่อเมืองที่สนามบินตั้งอยู่ (ภาษาอังกฤษ)**",
        placeholder="เช่น London"
    )

with col3:
    continent = st.selectbox(
        "**ทวีป**",
        options=[""] + CONTINENTS
    )

# กำหนด Session State เริ่มต้น
if 'data_consistent' not in st.session_state:
    st.session_state.data_consistent = False
if 'distance_km' not in st.session_state:
    st.session_state.distance_km = 0
if 'evaluation_df' not in st.session_state:
    st.session_state.evaluation_df = None

# ปุ่มตรวจสอบความสอดคล้อง
if st.button("🔎 ตรวจสอบข้อมูลสนามบิน", disabled=not is_gemini_ready or not (icao_code and city_name and continent)):
    st.session_state.distance_km = 0  # รีเซ็ตค่าเมื่อตรวจสอบใหม่
    st.session_state.evaluation_df = None # รีเซ็ตตาราง
    
    if is_gemini_ready:
        # 1.2 ให้ Gemini เช็คความสอดคล้อง
        with st.spinner("กำลังตรวจสอบข้อมูลกับ Gemini..."):
            # เรียกใช้โดยไม่ส่ง client
            consistency_result = check_airport_consistency(icao_code, city_name, continent)

        if consistency_result.startswith("PASS"):
            st.success("✅ ข้อมูลสนามบินสอดคล้อง! ดำเนินการขั้นตอนถัดไป")
            st.session_state.data_consistent = True
        elif consistency_result.startswith("FAIL"):
            st.session_state.data_consistent = False
            error_message = consistency_result.replace("FAIL:", "").strip()
            st.error(f"❌ ข้อมูลไม่สอดคล้อง: {error_message}")
        elif consistency_result.startswith("API_ERROR"):
            st.session_state.data_consistent = False
            st.error(consistency_result)
        else:
            st.warning(f"⚠️ ไม่สามารถตีความผลลัพธ์การตรวจสอบได้: {consistency_result}.")
            st.session_state.data_consistent = False



# --- 6. ส่วนคำนวณและแสดงผลเครื่องบิน (2. เลือกรุ่นเครื่องบิน) ---

if st.session_state.data_consistent:
    st.header("2. การประเมินเส้นทางบินและรุ่นเครื่องบิน")

    # 2.1 ค้นหาระยะทางบิน
    if st.session_state.distance_km == 0:
        with st.spinner(f"กำลังค้นหาระยะทางบิน BKK ไป {icao_code}..."):
            distance = get_flight_distance(icao_code)
            st.session_state.distance_km = distance
    else:
        distance = st.session_state.distance_km

    if distance > 0:
        st.info(f"📏 **ระยะทางบิน (BKK -> {icao_code}):** **{distance:,} กิโลเมตร**")

        # 2.2 & 2.3 การประเมินเครื่องบินและการแสดงผล
        if st.session_state.evaluation_df is None:
            with st.spinner("กำลังให้ Gemini คาดการณ์ Demand และประเมินความเหมาะสมของเครื่องบิน..."):
                csv_result = get_aircraft_evaluation(distance, icao_code, city_name)
                
            if csv_result and not csv_result.startswith("API_ERROR"):
                try:
                    # แปลง CSV ที่ได้จาก Gemini เป็น DataFrame
                    df = pd.read_csv(io.StringIO(csv_result), header=None)
                    
                    if df.shape[1] == 11:
                        df.columns = [
                            "ชื่อรุ่นเครื่องบิน", "พิสัยการบิน (กม.)", "จำนวนที่นั่ง (eco/bc/first)", 
                            "อัตราสิ้นเปลือง (USD/hr)", "คาดการณ์ผู้โดยสารขาไป (eco/bc/first)", 
                            "คาดการณ์ผู้โดยสารขากลับ (eco/bc/first)", "ความถี่เที่ยวบิน (ไป+กลับ)/สัปดาห์", 
                            "เวลา Departure จาก BKK", "เวลา Departure จากปลายทาง", 
                            "ความเหมาะสม (ดาว)", "สรุปสาเหตุ"
                        ]
                        st.session_state.evaluation_df = df
                    else:
                        st.error(f"❌ Gemini ส่งข้อมูลกลับมาไม่ครบตามรูปแบบ (คาด 11 คอลัมน์ ได้ {df.shape[1]} คอลัมน์). โปรดลองอีกครั้ง")
                except Exception as e:
                    st.error(f"❌ เกิดข้อผิดพลาดในการประมวลผลข้อมูลจาก Gemini: {e}")

        if st.session_state.evaluation_df is not None:
            st.subheader("ตารางสรุปการประเมินรุ่นเครื่องบิน")
            
            # ฟังก์ชันช่วยในการแสดงผลดาว
            def format_star(score):
                score = float(score)
                if score == 0:
                    return "🚫 0.0 ดาว (บินไม่ถึง)"
                full_stars = int(score)
                half_star = "½" if score - full_stars >= 0.25 and score - full_stars < 0.75 else ""
                stars = "★" * full_stars
                return f"{stars}{half_star} ({score:.1f})"
            
            display_df = st.session_state.evaluation_df.copy()
            # ตรวจสอบและจัดการข้อมูลที่อาจจะไม่ใช่ตัวเลขในคอลัมน์ความเหมาะสม
            try:
                display_df['ความเหมาะสม (ดาว)'] = display_df['ความเหมาะสม (ดาว)'].astype(str).str.replace(' ', '').str.replace(',', '').apply(format_star)
            except Exception:
                 st.warning("⚠️ มีปัญหาในการแสดงผลคะแนนความเหมาะสม (ดาว) อาจมีค่าที่ไม่เป็นตัวเลข")
            
            st.dataframe(
                display_df,
                height=350,
                use_container_width=True,
                column_config={
                    "สรุปสาเหตุ": st.column_config.Column(
                        "สรุปสาเหตุ (50-100 คำ)",
                        help="สรุปเหตุผลการให้คะแนนความเหมาะสม",
                        width="large",
                    ),
                    "พิสัยการบิน (กม.)": st.column_config.NumberColumn(
                        "พิสัยการบิน (กม.)",
                        format="%d",
                    ),
                }
            )

            # 2.4 สร้าง dropdown ให้ user เลือก
            st.subheader("3. เลือกรุ่นเครื่องบินที่ต้องการใช้")
            
            # กรองเครื่องบินที่ได้ 0 ดาวออกหรือไม่
            available_aircraft = st.session_state.evaluation_df[st.session_state.evaluation_df['ความเหมาะสม (ดาว)'].astype(float) > 0.0]["ชื่อรุ่นเครื่องบิน"].tolist()
            
            if not available_aircraft:
                 st.error("🚨 ไม่มีรุ่นเครื่องบินใดในรายการที่สามารถบินในเส้นทางนี้ได้! (ทุกรุ่นได้ 0 ดาว)")
            else:
                aircraft_selection = st.selectbox(
                    "**เลือกรุ่นเครื่องบินสำหรับเส้นทางนี้**",
                    options=available_aircraft
                )

                if aircraft_selection:
                    selected_data = st.session_state.evaluation_df[
                        st.session_state.evaluation_df["ชื่อรุ่นเครื่องบิน"] == aircraft_selection
                    ].iloc[0]

                    st.success(f"✅ คุณเลือกรุ่น **{aircraft_selection}** แล้ว!")
                    
                    # ค้นหาค่าดาวที่ถูกจัดรูปแบบแล้ว
                    selected_star = display_df[display_df["ชื่อรุ่นเครื่องบิน"] == aircraft_selection]['ความเหมาะสม (ดาว)'].iloc[0]
                    
                    st.markdown(f"""
                    * **ความเหมาะสม:** **{selected_star}**
                    * **พิสัยการบิน:** {selected_data['พิสัยการบิน (กม.)']} กม.
                    * **ความถี่ที่แนะนำ:** {selected_data['ความถี่เที่ยวบิน (ไป+กลับ)/สัปดาห์']} เที่ยวบินต่อสัปดาห์
                    * **เวลา Departure BKK:** {selected_data['เวลา Departure จาก BKK']}
                    * **สรุปสาเหตุ:** {selected_data['สรุปสาเหตุ']}
                    """)
        else:
            st.warning("⚠️ ไม่สามารถแสดงตารางประเมินได้เนื่องจากเกิดข้อผิดพลาดในการรับข้อมูลจาก Gemini.")
    else:
        st.error(f"❌ ไม่สามารถคำนวณระยะทางบินจริงจาก BKK ไป {icao_code} ได้ หรือระยะทางเป็น 0. โปรดตรวจสอบ ICAO Code และลองอีกครั้ง")